Two intervention/condition descriptions come from the SAME paper's experiments. Decide whether
they denote the SAME manipulation (the same knob being varied, or the same experimental arm)
merely worded differently — or DIFFERENT interventions.

SAME (merge): "test-time training" vs "TTT"; "2x2 patch on a 64x64 canvas" vs "2x2
patchification"; "ensemble of an 18M ViT and a 55M U-Net, each with TTT" vs "we ensemble one ViT
and one U-Net, each with test-time training".

DIFFERENT (keep separate): "vary offline data scale" vs "vary task diversity"; "ViT" vs "U-Net";
"single-view" vs "multi-view"; "data scaling" vs "task diversity scaling".

When unsure, answer no (never over-merge — the raw nodes are always kept).

A: {a}
B: {b}

Answer with exactly ONE word: yes (same) or no (different).
